# saifullahisonline

Portfolio of Saifullah Muhamad — System Analyst & Business Analyst.

Static site: `index.html` + `assets/` + `lib/` (GSAP, ScrollTrigger, Lenis).
Vercel runs `npm run build`, which copies them into `dist/`.
